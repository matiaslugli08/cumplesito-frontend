services:
  - type: web
    name: cumplesito-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://cumplesito-backend.onrender.com/api
